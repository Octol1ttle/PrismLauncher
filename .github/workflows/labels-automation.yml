name: Run automatic labeling

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

on:
  push:
    branches:
      - develop
      - labels-actions-script # TODO remove
  pull_request_target:
    types:
      - opened
      - synchronize

permissions: {}

jobs:
  run-labels-script:
    name: Run script
    runs-on: ubuntu-slim
    #if: github.repository_owner == 'PrismLauncher' TODO uncomment

    steps:
      - name: Generate token
        id: generate-token
        uses: actions/create-github-app-token@v3
        if: github.repository_owner == 'PrismLauncher' # TODO remove
        with:
          app-id: ${{ vars.PULL_REQUEST_APP_ID }}
          private-key: ${{ secrets.PULL_REQUEST_APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v7
        with:
          sparse-checkout: '.github/scripts'

      - name: Run labeling script
        uses: actions/github-script@v9
        with:
          github-token: ${{ steps.generate-token.outputs.token || github.token }}
          retries: 3
          script: |
            const script = require('.github/scripts/labels.js')
            await script({github, context, core})